# Android plugins

Only audited Godot 4.5-compatible Android plugins belong here. Plugin binaries, licenses, Maven dependencies, SDK versions, consent requirements, and data-safety declarations must be reviewed before a release. Signing files and credentials are never committed.
